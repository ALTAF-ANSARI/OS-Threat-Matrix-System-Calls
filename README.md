# OS-Threat-Matrix-System-Calls
A secure MERN-stack platform with native C++ Win32 system call integration. Features sandboxed file &amp; process management, role-based access control, JWT authentication, audit logging, and an admin log viewer dashboard.
